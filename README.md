# Running locally

1. Install Docker
2. Copy the `.env.dev` file provided by the team into `./backend/.env.dev`.
3. Run `docker compose down; docker compose build; docker compose up -d` in the project root
4. Go to http://localhost and enjoy!